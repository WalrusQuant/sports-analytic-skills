# Honest Label Rules

Every key figure must show:

- title stating what was measured
- period (seasons/dates)
- n (games/events/rows)
- metric definition if not obvious
- baseline reference when comparing models

If uncertainty is unknown, say unknown — do not invent error bars.
